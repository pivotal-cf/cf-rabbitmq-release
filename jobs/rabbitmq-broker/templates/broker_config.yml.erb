<%
rabbitmq_broker_config = p("rabbitmq-broker")

rabbitmq_broker_config["ip"] = link("rabbitmq-broker").instances.first.address
rabbitmq_broker_config["rabbitmq"]["hosts"] = link("rabbitmq-haproxy").instances.map(&:address)
%>

<%= rabbitmq_broker_config.to_yaml %>
