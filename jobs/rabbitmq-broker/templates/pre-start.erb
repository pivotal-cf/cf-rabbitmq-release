#!/bin/sh

set -eux

INIT_LOG_DIR=/var/vcap/sys/log/rabbitmq-broker

ensure_dir() {
    _dir=$1
    mkdir -p "${_dir}"
    chown -R vcap:vcap "${_dir}"
    chmod 755 "${_dir}"
}

ensure_dir "${INIT_LOG_DIR}"
