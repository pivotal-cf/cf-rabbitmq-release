#!/bin/bash -e

[ -z "$DEBUG" ] || set -x

ensure_directories_are_not_world_readable() {
  chmod 750 "/var/vcap/jobs/smoke-tests"
  chown -LR vcap:vcap "/var/vcap/jobs/smoke-tests"
}

ensure_directories_are_not_world_readable
